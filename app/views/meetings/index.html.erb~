<% title "Meetings" %>

<table>
  <tr>
    <th>Trainer Meeting</th>
    <th>Type</th>
    <th>Time Slot</th>
    <th>Date</th>
    <th>Duration</th>
  </tr>
  <% for meeting in @meetings %>
    <tr>
      <td><%=h meeting.trainer_meeting_id %></td>
      <td><%=h meeting.type_id %></td>
      <td><%=h meeting.time_slot_id %></td>
      <td><%=h meeting.date %></td>
      <td><%=h meeting.duration %></td>
      <td><%= link_to "Show", meeting %></td>
      <td><%= link_to "Edit", edit_meeting_path(meeting) %></td>
      <td><%= link_to "Destroy", meeting, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Meeting", new_meeting_path %></p>
